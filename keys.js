const computer = {
    brand: 'lenovo',
    price: 35000,
    processor: 'intel',
    hdd: '512gb'
}

const keys = Object.keys(computer);
const keysCount = Object.keys(computer).length;
console.log(keys);
console.log(keysCount);